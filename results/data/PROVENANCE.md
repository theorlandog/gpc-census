# PROVENANCE
Constraint systems ranks 9-10: parsed from M. Altunbulak, PhD thesis (Bilkent, 2008), Appendix A;
counts validated against Altunbulak-Klyachko CMP 282, 287 (2008): 52/60/93/125/161.
(3,9): 52 inequalities, 58 vertices.
Vertices: exact enumeration (lrslib, rational arithmetic). Validation: embedding coherence
(rank r -> r+1), frozen-core lifts, particle-hole self-duality of (4,8) and (5,10) at
inequality/vertex/verdict level, agreement with published rank-8 vertex tables and (4,9)=103.
Census verdicts: two-stage solver certificates (CP-SAT integer stage, ortools 9.15.6755;
CBC real stage). (3,10) vertex 89 resolved with extended real-stage budget (7200 s).
The 11 negative verdicts in Proposition 1 and the v_B verdict in Theorem 2
additionally carry standalone exact global certificates in
interference_certificates.json. The remaining 144 INTERFERENCE verdicts retain
the solver-level classification evidence described above.
Rank-10 constraint lists are conjecturally complete per AK2008; rank-10 rows conditional.
Verdicts transport to dual systems (4,7),(5,8),(5,9),(6,9),(6,10),(7,10) by particle-hole
complement bijection on determinants.

## holonomy_two_elementary.json (2-elementarity reduction and Theorem A')

Generated by `scripts/holonomy_two_elementary.py` from
`results/data/holonomy_fields.json`, guarded by
`tests/test_holonomy_two_elementary.py`, documented in
`docs/holonomy_two_elementary.md`. It reduces 2-elementarity of the holonomy
field to two conditions on `cos^2 Phi`: R1, that `deg_Q(cos^2 Phi) <= 2`; and
R2, that for `deg_Q(cos Phi) = 4` the norm `N(cos^2 Phi)` is a square in Q.
Together they force the Galois group into C1, C2 or V4. Theorem A' is the
consequence at one-hop classes of size at most two, where Theorem A's formula
makes `cos Phi = n / sqrt(M)` with `n, M` rational, so `cos^2 Phi` is rational
and R1 holds at degree one.

Row added by the completeness audit backfill: the artifact shipped with a
checksum, a generator and a guard test but no provenance row, which is exactly
the gap `scripts/audit_data_completeness.py` exists to catch.

## lambda3_c7_class_viii_witness.json (exact orbit-closure witness)

EXACT. `scripts/lambda3_c7_class_viii_witness.py` records a six-term signed
state with squared amplitudes `(2,1,1,1,2,2)/9`. Symbolic replay gives norm
one, diagonal 1-RDM spectrum `(5/9)^3,(1/3)^4`, characteristic polynomial
`(x-5/9)^3(x-1/3)^4`, full one-body rank 7, and GL_7 orbit tangent rank 34.
The distinct orbit dimensions in the finite Lambda^3 C^7 classification
therefore put the state in class VIII. Adding this attained point to the exact
inner hull closes the sole unreached outer vertex, so the class-VIII verdict
in `lambda3_c7_polytopes.json` is EXACT. The discovery search examined 590
signed support skeletons, but search completeness is not used in the proof.
`scripts/verify_lambda3_c7_class_viii_witness_standalone.py` imports no project
module and independently reconstructs wedge signs, the 1-RDM, characteristic
polynomial, and all 49 orbit-tangent columns.

## conditional_2rdm_program.json (complex-flux benchmark correction)

NUMERICAL_WITH_EXACT_MODEL_INPUT for the physical benchmark; exact where the
artifact separately labels symbolic flux and support-function statements. The
selected spinless three-fermion, six-orbital t-V ring has `t=1`, `V=2`, on-site
gradient `7/20`, and boundary phase `(4+3i)/5`. It passes the frozen ground-gap,
occupation-gap, natural-basis, dominant-carrier, gauge-invariant flux, and
quasipinning-containment gates. Five of the 12 scan points have usable genuine
complex flux.

The complex phase exposed a convention error invisible for real eigenvectors.
For the stored `gamma[p,q]=<a_p^dag a_q>` convention, passive natural-orbital
CI coefficients use the third exterior power of `U.T`, while transformed
integrals use physical columns `U.conj()`. The previous `U.conj().T` coefficient
formula leaves a 0.169 off-diagonal 1-RDM residual at the selected point. The
correct formula leaves about 2.2e-16. The quasipinning enlargement was also
corrected to use the centered norm of the full fixed-particle Hamiltonian;
`P H P` alone cannot bound carrier-to-complement matrix elements.
`scripts/verify_complex_flux_physical_benchmark_standalone.py` imports no
project module, builds the lattice Hamiltonian directly in occupation space,
constructs the exterior basis transform from minors, and passes 15 independent
checks, including the old-conjugation negative control.

## interference_certificates.json (EXACT global no-design certificates)

Generated by `scripts/generate_interference_certificates.py` for every negative
case in Proposition 1 and for v_B in Theorem 2. For an augmented determinant
incidence column a_T and target b = (lambda, 1), each primitive integer vector
y with y dot b > 0 defines the exact hitting clause

    P_y = {T : y dot a_T > 0}.

Every nonnegative incidence realization must use a determinant in P_y. The
artifact closes these clauses under permutations inside equal-spectrum blocks
and supplies a finite branch DAG proving that no one-hop-free support hits all
clauses. Totals: 12 records, 192 primitive integer vectors, 5141 expanded
clauses, and 226 proof nodes at maximum depth 8.

`scripts/verify_interference_certificates_standalone.py` uses only the Python
standard library and shares no code with the generator. It reconstructs the
incidence system, exact rational pairings, symmetry action, one-hop conflict
graph, and every branch. Solver runs remain discovery and cross-check evidence
only; the verifier does not invoke a solver.

Which vectors enter the artifact depends on the floating HiGHS solutions the
generator discovers, so `generate_interference_certificates.py --check` is a
same-toolchain reproducibility check, not a portable one. The artifact's
validity does not depend on it: the test suite runs the verifier, never the
generator, and any exact certificate set that verifies proves the same claim.

## fiber_symmetries.json (pair coordinates and exact v_B invariants)

EXACT. `scripts/fiber_symmetries.py` constructs the pair-by-determinant
incidence matrix for every shipped support and records one nonzero integer
maximal minor for each. The identity

    sum_{j != i} (B_S w)_{ij} = (N-1)(A_S w)_i

shows that full column rank of B_S makes pair occupations injective on every
incidence-kernel weight deformation. All 799 supports pass, spanning 403
transport classes. The positive-kernel population is 63 records in 39
transport classes, with zero pair-map failures. This is a certified atlas
fact, not a theorem for arbitrary supports; signed combinatorial 2-trades are
abstract counterexamples.

The same generator expands the v_B 2-RDM Gram matrix symbolically on its exact
fixed-spectrum family. Fixed-spectrum, not fixed-diagonal: every member has
1-RDM spectrum exactly lambda_vB, but in the frame the support is written in
rho keeps an off-diagonal entry that moves along the family (at t=0,
rho_48 = -5/92 - sqrt(119)*i/92). The natural-orbital frame is the separate
states_natural_orbital.jsonl ledger. It proves

    tr(Gamma_2^2) = 1438/529,
    tr(Gamma_2^3) = 6(2899-8t)/12167.

The cubic moment recovers the family parameter under the full one-body group
and separates the two Q(sqrt(35)) Galois walls. At t=0, exact spectral
projectors give fixed-spectrum constraint rank 7 and orbital-phase gauge rank
7 in 16 real amplitude coordinates. Appending the cubic and quartic 2-RDM
moment differentials raises the rank to 9; the artifact records the nonzero
9 by 9 minor

    299925504*sqrt(8211)/1035662780341225.

Thus those two moment differentials form a basis of the two-dimensional
quotient cotangent at that point. This last conclusion is first order only:
global integrability and injectivity on the second surface direction remain
open. `scripts/verify_fiber_symmetries_standalone.py` re-derives all 799
integer minors from states.jsonl with standard-library Bareiss elimination;
for v_B it only checks the recorded constants, so it is a second derivation of
the pair layer and a consistency check on the moment layer. The v_B moment
identities get their second derivation in `tests/test_fiber_symmetries.py`,
which rebuilds the family from the published weights and coherence equation,
contracts it with the repository's own pair-vector code, and reproduces both
trace formulas numerically without touching the generator's symbolic path.

## states.jsonl (constructed extremal states)

Per-vertex extremal states, produced by the routed state pipeline
(scripts/solve_all.py --all): DESIGN-INT vertices built directly from their
classification witness (exact by construction), DESIGN-REAL and INTERFERENCE
vertices through the weights-first block/clique solver (max_clique=3), certified
by exact characteristic-polynomial identity where the closed form is recognized
(status EXACT), else labeled TIER-C (exact numeric state, closed form not yet
recognized) or FAIL (outside the current ansatz family). Each record carries the
classification verdict, so the dataset cross-checks census_master.csv.

This file is COMPLETE: all 799 vertices of the nine determinate systems
(ranks 6 to 10) carry a certified closed-form extremal state. Regenerated fresh
it is deterministic up to the interference phase gauge, which does not affect
the certified spectrum.

Correction (v0.11): three DESIGN-REAL records, (3,9) index 53, (3,10) index 106,
and (4,10) index 53, shipped valid extremal states on one-hop-connected supports
rather than genuine design witnesses. They were replaced by design witnesses
transported (padding / frozen-core lift) from the (3,8) parent design at
denominator 12, each verified by the exact characteristic-polynomial identity;
their feature_table.csv rows (one-hop-class and max-class-size counts) were
regenerated to match.

## rdm_observability_census.json (exact pure-state reconstruction atlas)

EXACT support-combinatorial certificates generated by
scripts/rdm_observability_census.py from states.jsonl. For each of the 799
shipped determinant carriers, the artifact computes singleton and pair
incidence ranks by exact rational row reduction, stores an invertible pair
incidence minor when one exists, enumerates signed k-RDM coherence channels
through the first connected order, and stores exact summaries and a
collision-free spanning tree when one exists. For the 771 order-two records,
the 2-RDM tree and incidence minor provide a rational inverse to the complete
rank-one carrier coefficient matrix for pure states with nowhere-vanishing amplitudes on
the listed support in the fixed orbital frame. The other 28 trees certify
reconstruction only after supplying the stated order-three, order-four, or
order-five coherences.

The exact totals are 799 of 799 full-column-rank pair-incidence matrices and
771 of 799 connected collision-free 2-RDM graphs: 603 DESIGN-INT, 12
DESIGN-REAL, and all 156 INTERFERENCE records. The 28 unresolved records are
not classified as non-injective. A disconnected collision-free graph only
means this sufficient certificate is silent. The record supports contain 305
unordered one-hop determinant pairs in total, and every corresponding channel
is included. The independent script
scripts/verify_rdm_observability_standalone.py rechecks the input hash, every
rank and determinant certificate, every signed tree channel, connectivity,
and all aggregate counts without importing the generator or project package.

## vb_carrier_hamiltonian.json (exact scheduled v_B dynamics)

EXACT algebraic certificate generated by scripts/vb_carrier_hamiltonian.py.
The calculation uses the conic \(s^2=119+70t-85t^2\), exact differentiation
along a wall-regular periodic clock, signed second quantization on the full
126-dimensional \(N=4,d=9\) basis, and polynomial reduction modulo the conic.
It proves that a Hermitian pair kernel with a seven-entry symbolic support
preserves the complete eight-determinant \(v_B\) carrier and satisfies the
scheduled Schrödinger equation exactly.

The artifact also proves that the specified orbital lift has one constant
complete 1-RDM, verifies a unimodular pair-incidence minor and connected
seven-edge 2-RDM reconstruction tree, and records the resulting exact closed
nonautonomous TD-2RDM consequence. The independent script
scripts/verify_vb_carrier_hamiltonian_standalone.py rebuilds the full-sector
operator, carrier leakage test, both Schrödinger identities, fixed 9 by 9
1-RDM, reconstruction certificate, and the basis-free identity
\(\operatorname{Tr}(\Gamma_2^3)=6(2899-8t)/12167\) without importing the
generator or project package. The invariant has nonzero derivative and proves
the trajectory is not a one-body orbital orbit. The result is a precomputed
schedule, not an autonomous Hamiltonian or a mixed-state closure.

Update (v0.12): the (4,9) index 65 record (v_B = (20,14,14,14,14,4,4,4,4)/23)
now ships the REAL extremal state: the theta=0 endpoint of the wall family
k(t) = (1+t, 8-t, 4, 3, 2-t, 2+t, 1, 2)/23 on the same support, at
t0 = 7/17 - 18*sqrt(35)/85 (reality wall 85 t^2 - 70 t - 119), squared
weights in Q(sqrt(35)). Generated and certified by scripts/vb_real_state.py
(exact characteristic-polynomial identity; independently re-verified by
scripts/verify_states_standalone.py). closed_form.weights for this record
holds exact radical strings instead of integers; its feature_table.csv
all_real flag is now 1. The previously shipped phase-carrying fiber point is
the t=0 rational anchor of the same family, recoverable from the family
metadata in the record.

## cascade.jsonl, cascade_summary.json (fixed-spectrum sparsification cascade)

NUMERICAL, not certified. One record per vertex from scripts/census_cascade.py,
which runs gpc_census.cascade over every state of states.jsonl: continuation
along the FIXED-SPECTRUM fiber driving the smallest surviving amplitude to zero
against the minimal-polynomial certificate prod_i (rho - lambda_i I) = 0 taken
over distinct eigenvalues, with the orbital-phase gauge pinned and each accepted
round gated on the eigenvalue multiplicities as well as the certificate.

The field support_upper is the smallest support reached. Read it alongside
support_certified: the certified support is an UPPER BOUND on the minimal
support, and support_upper is a better upper bound where the cascade landed a
drop. The two bound DIFFERENT quantities: support_certified bounds s_Q^NO (its
state has rho = diag(lambda)) while support_upper bounds only s_Q^free, since
no cascade endpoint has a diagonal 1-RDM. See orbit_check.json. Neither is a minimality claim, and support_upper is not
certified: support_upper_evidence records "numerical" on every record, and the
endpoint amplitudes are shipped (final_amplitudes_real, final_amplitudes_imag,
final_support_dets) so the endpoint can be rechecked without rerunning the
search. Fiber tangent dimensions are written under fiber-qualified keys
(fixed_spectrum_*, fixed_rho_*); the two are different objects.

## oos_bracket.json (out-of-sample bracket holdout)

Scored pre-registration, scripts/oos_bracket.py against the predictions in
docs/prereg_bracket_3_11_3_12.md, which were committed before the script was
run. Holdout: 19 certified true vertices of (3,11) and their 19 (3,12) face
embeddings. Result 4 PASS, 2 FAIL, both FAILs at (3,11) v43. NUMERICAL.

## reconciliation.json (superseded-claim reconciliation)

scripts/reconcile_claims.py re-derives the four claims this session corrects
(v103 rigidity by fiber, the isolated support-14 reading, the support column as
an upper bound, and the DESIGN-REAL denominator ratio count) so each correction
points at a measurement rather than at prose. The denominator-ratio item is
exact rational arithmetic; the rest are numerical.

## cascade_exact.json, v103_support10_certificate.json (EXACT support bounds)

EXACT, in contrast to cascade.jsonl. scripts/exactify_cascade.py refines each
cascade endpoint in high-precision arithmetic, recognizes rational or
quadratic-algebraic squared weights by integer relation detection, gauge-fixes
the phases, and verifies the
characteristic-polynomial identity det(rho - x I) = prod_m (n_m/D - x) in exact
rational and radical arithmetic. An entry is marked exact only when the weights
sum to 1, the state is real up to the orbital-phase gauge, and the exact identity
holds; every other status is reported with the reason it fell short and its
bound stays numerical.

All 71 sparsified endpoints are exact on the fixed-spectrum locus. The final two,
(5,10) index 144 and index 256, share the field Q(sqrt(1065)); each has eight
exact squared weights, an exact characteristic-polynomial identity, and
nonzero off-diagonal 1-RDM entries. They therefore certify s_Q^free bounds, not
s_Q^NO bounds.

v103_sqfree_certificate.json (CURRENT) certifies the support-10 state over
(3,10) index 103 under the claim that actually holds, s_Q^free <= 10: exact
rational weights summing to 1, the characteristic-polynomial identity
det(rho - xI) = (x - 9/17)^4 (x - 5/34)^6 in exact arithmetic, the single loop
holonomy equal to pi so the state is real up to gauge, and BOTH nonzero
off-diagonal 1-RDM entries reported exactly, rho[3,9] = -5/68 and
rho[1,6] = -sqrt(42)/221. Those two entries are the reason the bound is
s_Q^free rather than s_Q^NO, and both are pinned by
scripts/check_manuscript_counts.py because the manuscript names both.

v103_support10_certificate.json is the same result for (3,10) index 103 by an
independent standalone route, kept separate because it is the instance that
changes a published claim: support 14 in the library, support 10 certified here,
state denominator 46852 = 2^2*13*17*53. The two routes agree on support,
weights and denominator; their sign patterns differ by a gauge choice and both
satisfy the exact identity.

## v103_endpoint.json (high-precision endpoint)

The 60-digit refinement of the v103 support-10 endpoint with its
integer-relation recognition, kept as the numerical input the exact certificate
was derived from.

## gauge_min.jsonl, gauge_min_summary.json (natural-orbital gauge minimization)

Produced by scripts/gauge_census.py (engine src/gpc_census/gauge.py). One record
per vertex: the library support, the exact gauge-invariant lower bound (occupied
block-profile classes, refined by per-class flattening ranks), the support
reached by minimizing over the natural-orbital gauge family
U(m_1) x ... x U(m_k), and the acceptance gate on any state that moved.

The headline is a null result with an exact majority: no state of the 799 admits
a support-reducing rotation in its own gauge family, and for 670 of them the
library support attains the lower bound, so gauge-minimality is certified by a
rank computation rather than by a search. Exterior/Koszul contraction maps
strictly strengthen 82 records and newly close 41. Floating ranks only select
the contraction map: all 799 published bounds are rerun by exact sparse
symbolic elimination before publication (exterior_certificates_exact in the
summary, arithmetic: exact on every per-class certificate), and a disagreement
between the two is a hard error rather than a downgrade. The other 129 are open
in both directions. The absence of a reduction is a search result, whose power
is pinned by the recovery test in tests/test_gauge.py.

## rank11_contraction.json (normalized rank-11 distance audit)

NUMERICAL, with an exact conditional hyperplane calculation.
scripts/rank11_contraction_audit.py minimizes the normalized objective
||gamma(c)/||c||^2 - diag(lambda)||_F^2 for real and complex amplitudes. The
uniform (3,11) vertex is the zero-distance control. All four OPEN bracket
candidates retain positive numerical floors. Candidate 44 lands on the spectrum
(37/72,(29/60)^4,(7/72)^5,1/15), whose exact trace-hyperplane distance from the
target is 11/4320 conditional on the CLAIMED level-5 inequality being valid.
This artifact is evidence and a corrected target datum, not a proof of that
inequality or of non-attainability.

These records bound s_Q^NO, the minimum support over states with rho exactly
diag(lambda). They are NOT comparable with the cascade's free-basis bounds.

## orbit_check.json (2-RDM gate on the cascade endpoints)

Produced by scripts/orbit_check.py. The 2-RDM spectrum is invariant under every
one-body unitary, so it decides whether a cascade endpoint is a new state or the
library state in different orbitals. Over all 71 endpoints: 63 agree with the
library state to ~1e-16 (same state, rotated), 8 differ by 3.4e-3 to 4.7e-2 (no
one-body rotation can do that, so they are genuinely different extremal states),
and ZERO have a diagonal 1-RDM, which is why every cascade bound is a bound on
s_Q^free and not on the natural-orbital quantity. The v103 entry carries the
explicit connecting unitary (residual 7.2e-16, off-block entries 0.196, so a
U(10) rotation and not a gauge one). Every one of the 63 same-state endpoints
ships its rotation matrix, all verified, worst residual 1.7e-15.

## holonomy_fields.json (gauge-invariant loop holonomy fields)

EXACT, with an independent numerical cross-check. scripts/holonomy_field_scan.py
walks every certified INTERFERENCE state whose support carries loops (63 states,
82 independent loops) and records, per loop: the integer kernel vector, the
exact holonomy cosine, its minimal polynomial, its degree, its Galois group
computed with sympy galois_group rather than inferred from the shape of the
polynomial, the extracted square-root radicands, and a PSLQ integer-relation
recognition performed at 120 digits and RE-VERIFIED at 240, with the
verification residual recorded on every row. A relation that fails
re-verification is deleted, not downgraded; the discriminating evidence for that
protocol is reproduced in the precision_lesson block.

Result: all 82 degrees lie in {1, 2, 4} (37 / 32 / 13) and all 82 Galois groups
are 2-elementary (C1 / C2 / V4). The loop-kernel bases are certified saturated,
so no holonomy relation is missed. The channel_hypotheses block verifies the
hypotheses the theorem of docs/holonomy_rationality.md runs on: all 84 one-hop
channels have rational |rho_AB|^2, and for all 63 states the within-channel
difference vectors generate the loop lattice, saturated over Z. The
trisection_audit block shows the pipeline's TRISECTED phase tier is a property
of how the amplitude phase is printed, not of the state: no cubic subextension
occurs anywhere in those 13 states.

## denominator_arithmetic.json (where a state denominator comes from)

EXACT. scripts/denominator_arithmetic.py splits the state denominator into an
incidence stage (solve A_S p = lambda, sum p = 1 over Q, with the Smith
invariant factors of A_S recorded) and a pinning stage (the one-hop channel
conditions restricted to the incidence solution space). Worked examples at v89,
where the silence condition on the incidence line loses its quadratic terms and
the surviving linear equation 130 p9 - 1 = 0 mints the prime 5, and at v103,
where two channel equations have identical quadratic parts and their difference
13 p12 = p9 + p13 carries the minted prime 13.

The census block scores the predictions pre-registered in
docs/prereg_denominator_arithmetic.md: P-DEN-2 FAIL (18 exceptions, mechanism
recorded), P-DEN-3 FAIL as predicted (1 exception, v103, pinned as predicted),
P-DEN-4 PASS (736 states, 0 exceptions). The (4,9) index 65 record is excluded
from the ratio predictions: its weights are not rational, so it has no state
denominator.

## two_block_family.json (the two-block family, and the cross-block rule)

MIXED, labeled per field. Channel splits, block structure and transport
classes are EXACT combinatorics; tangent dimensions are NUMERICAL, inherited
from cascade.jsonl. scripts/two_block_family.py assembles the four two-block
INTERFERENCE vertices in full (channels split into within-block and
cross-block, both fiber tangents, holonomy fields, denominator arithmetic,
gauge-minimality status, cascade behaviour) and scores the predictions
pre-registered in docs/prereg_two_block_inversion.md.

Result: a CROSS-BLOCK one-hop channel is NECESSARY for the fixed-rho and
fixed-spectrum tangent dimensions to differ, on all 799 states across 403
transport classes with zero exceptions. The converse is false at 82 states,
all with incidence kernel 0. The two proposed counting rules (gap = X and
gap = 2X) are falsified. Independent scope is reported in TRANSPORT CLASSES
rather than row counts, per standing rule R2 of docs/prereg_template.md,
because padding and frozen-core siblings carry a state's fiber data unchanged.

The tangent_sector_split block decomposes v103's six fixed-spectrum
directions as 3 weight plus 3 phase, reproducing the silence-rank lemma's
formula term by term; at (3,10) v108 the sectors do not decouple, because the
state is genuinely complex and the lemma's real-state bookkeeping does not
apply there.

## oos_bracket.json: provenance_independence block (added 2026-07)

The scorecard was scored at 38 rows. A later audit traced every measurement to
its root origin and found 34 of them are transports or face embeddings of
rank-10 states drawn from 17 distinct donors, leaving 4 rows resting on 2
states actually constructed at the new rank. The provenance_independence block
records this per measurement, and every verdict now carries independent_scope
and independent_distinct_states beside its row scope, plus a corrected reading
alongside the original verdict. The original verdicts are unchanged; see the
PROVENANCE CORRECTION section of docs/prereg_bracket_3_11_3_12.md.

## attainer_gate_audit.json (diagonality gate over the whole library)

NUMERICAL, cross-checked in exact arithmetic at (3,10) v17.
scripts/attainer_gate_audit.py runs gpc_census.validate.check_vertex_attainer
over all 799 records. 645 pass (all 631 DESIGN-INT, all 12 DESIGN-REAL, and
the two cancellation-regime states v89 and v103); 154 fail, and they are
exactly the remaining INTERFERENCE records.

Every failing record still attains the spectrum (worst error 1.1e-15) at unit
norm, and none is a merely permuted relabelling: the failures are a basis
statement, not a wrong-state statement. The shipped certificate is the
characteristic-polynomial identity, which certifies spec(rho) = lambda and
asserts diagonality only for design states, where one-hop freedom gives it by
inspection. The operative consequence is that the support column of those 154
records bounds s_Q^free and not s_Q^NO.

## rigidity_rationality.json (does rigidity imply rational weights)

MIXED. Rigidity is NUMERICAL, inherited from cascade.jsonl; the weight field
is EXACT, recomputed from the closed form rather than read off the
integer-versus-string encoding. scripts/rigidity_rationality_census.py joins
the two per record and tallies the implication "fixed-rho first-order rigid
implies rational squared weights": 740 of 740 with zero counterexamples, the
single irrational record ((4,9) v65, weights in Q(sqrt 35)) being deformable.

The artifact carries a power_warning block that must travel with the number:
all 645 natural-orbital representatives are rigid and all 59 deformable
records fail the diagonality gate, so the implication has no
rigid-versus-deformable contrast among gate-passing records and its
discriminating power lies entirely in the non-natural-orbital population.

## states_natural_orbital.jsonl, natural_orbital_summary.json

NUMERICAL, and additive: it does NOT replace states.jsonl. For each of the 156
interference records, scripts/natural_orbital_ledger.py diagonalizes the
1-RDM as rho = U diag(w) U^H with w ordered to match lambda and pushes the
state through the N-th compound of U^T (the transpose, not the conjugate
transpose; the two agree for real rotations, so the wrong convention fails
only on the complex records). Every resulting state has 1-RDM exactly
diag(lambda) to 2.8e-16 and passes the attainer gate; the basis-free 2-RDM
spectrum is unchanged to 1.6e-15, so the rotation moved the basis and not the
state.

The cost is sparsity and exactness: support grows on 142 of 156 records and
never shrinks (mean 6.8 to 12.1, max 14 to 79), and the exact symbolic closed
forms do not survive the rotation, so the amplitudes here are floating point.
The two ledgers bound different quantities. states.jsonl keeps EXACT closed
forms on sparse supports whose 1-RDM is not diagonal for these records, so its
support column bounds s_Q^free; this ledger has diagonal 1-RDMs, so its
support column bounds s_Q^NO. Quote the right one.

## no_support_ratio.json (the trichotomy diagnostic)

NUMERICAL. scripts/no_support_ratio.py divides the natural-orbital support by
the sparse support for every record and scores the predictions of
docs/prereg_no_support_ratio.md, which carries an explicit disclosure that it
is NOT a blind pre-registration. P-NSR-1 FAILED as disclosed: 13 generic
INTERFERENCE records sit at ratio exactly 1. P-NSR-2 PASSED: no record has
ratio below 1. What survives is one-directional and clean: ratio > 1 implies
generic INTERFERENCE, 141 records with zero exceptions; the converse fails.

## vb_galois_orbit.json (the v_B Galois orbit test)

MIXED: the sign search is NUMERICAL at 1e-12, and the attaining pattern of
each embedding is re-verified in EXACT radical arithmetic with independent
fermionic-sign bookkeeping. scripts/vb_galois_orbit.py resolves the Galois
action on the wall parameter t rather than on nested surds, which is what the
earlier naive substitution got wrong. Both roots of 85t^2 - 70t - 119 give
positive weight vectors attaining the vertex exactly, each by 64 of the 128
sign patterns modulo global sign. Orbit size 2, weight field degree 2:
degree equals orbit size. The pre-registered prediction is CONFIRMED.

## uniqueness_census.json (gap b of the Rigidity-Rationality target)

NUMERICAL. scripts/uniqueness_census.py multi-starts the FIXED-RHO
realization system rho(c) = rho_0 on each state's own support, for twelve
fixed-rho rigid interference states, and counts converged solutions modulo
gauge. Targeting diag(lambda) instead would be infeasible for the 154 records
whose 1-RDM is not diagonal, which is why the system targets the state's own
rho. Solutions are clustered by distance on a gauge- and
conjugation-invariant fingerprint, reported three ways: weight vector ordered
(upper bound on the point count, since it also separates support-preserving
orbital permutations), weight multiset with holonomies (the point count), and
weight multiset alone.

Result: eleven of twelve have exactly one solution modulo gauge; v103 has
several, so the pre-registered uniqueness claim FAILS at one state. All twelve
have a UNIQUE weight multiset, so the multiplicity at v103 is a discrete phase
multiplicity and the weight vector is still Galois-fixed. Multi-start finds
solutions and cannot prove there are no others, so this bounds gap (b)
empirically rather than closing it; gap (a), isolation itself, is untouched.

## v103_fiber_count.json (the exact number of fiber points at v103)

NUMERICAL but EXHAUSTIVE rather than sampled. scripts/v103_fiber_count.py
answers how many points the v103 fixed-rho fiber has, and the answer is ONE.

The search space is what makes it exhaustible. Every solution carries the same
weight vector, so the unknowns are phases alone, and modulo the 9-dimensional
orbital-phase gauge the 14 phases leave exactly 5 degrees of freedom, one per
loop. That is a compact 5-torus of gauge-invariant holonomies, and a grid over
it can be exhausted instead of sampled: 7^5 = 16807 starts, every one
converging, all to a single point with holonomy (0, pi, pi, 0, 0), so the state
is real up to gauge exactly as the library record says. Worst 1-RDM
reconstruction error 5.7e-14.

This supersedes and retires an earlier probe, together with the reading built
on it, that reported v103 as carrying dozens of isolated fiber points and
climbing. That count was an artifact of a NON-INTEGER loop basis: amplitude
phases are read wrapped into (-pi, pi], and a gauge change followed by that
wrap shifts theta by 2*pi*m for an integer vector m, which moves an integer
holonomy by a multiple of 2*pi and leaves its cosine alone but moves a
real-basis holonomy arbitrarily. One physical point was being split into
dozens by branch choice. scripts/uniqueness_census.py now builds its loop
basis over the integers, and its verdict changes from FAIL to PASS on all
twelve sampled states.

## level5_ressayre_3_11.json (the (3,11) closure)

Generated by `scripts/level5_ressayre_3_11.py`. The four level-five rows
`lambda_A + lambda_11 <= 2` for the second-level quadruples
A in {2345},{1346},{1256},{1247}, published without proof in Klyachko
0904.2009 and CMP Remark 4.2.1 and carried in this census at tier CLAIMED,
are certified by the exact Ressayre screen in `gpc_census.generation`. Each
of the four remaining OPEN bracket candidates violates at least one of them,
so the (3,11) bracket closes at 19 TRUE + 31 REFUTED + 0 OPEN.

A certificate proves validity of its single inequality. Facetness,
irredundancy and completeness of the (3,11) system are separate questions and
the screen records each as not established; validity alone refutes a
candidate.

Calibration is complete at ranks 9 and 10, the largest published fixed-N=3
systems. Every subset row of size 2..7 with right side 1 or 2 was screened
(984 and 1,914 rows), and every certified row (19 and 31) was maximized by LP
over the published system in `src/gpc_census/data/constraints.json`: zero
invalid. Negative control on the same runs: 1,857 rows are demonstrably false
by that LP and none was certified. Recorded caveat, and the reason the
artifact carries the per-route breakdown: admissibility and the trace count
reject before the tangent determinant is formed, and only 6 of those 1,857
rows reached the determinant. A targeted hunt lowering the right side of all
145 published rank-9 and rank-10 facets by one and by two produced 290 further
false rows, all rejected before the determinant. The determinant step is
therefore the least stressed part of this calibration.

At rank 11 the four certified rows were checked against all 19 spectra the
census has certified TRUE there, plus the Slater and uniform points: no
violations.

The artifact carries the full certificate payload for each of the four rows,
and `scripts/verify_level5_ressayre_3_11_standalone.py` replays it using the
standard library only, sharing no code with `gpc_census.generation`. It
rebuilds the 165 weights, level sets, negative roots and tangent matrix from
the recorded `(h,z)` and evaluation point, derives the fermionic sign by
counting inversions rather than by tracking creation and annihilation
positions, and recomputes the determinant by exact `Fraction` elimination
rather than by the generator's Bareiss routine. 64 checks pass; the test suite
asserts the verifier fails on a tampered determinant or hyperplane.

`scripts/settle_bracket_3_11.py` carries LEVEL5-RESSAYRE as a fifth exact
method, applied after the four older ones so it decides only what they left
OPEN. `docs/bracket_3_11_settlement.json` regenerates at 19 TRUE / 31 REFUTED
/ 0 OPEN; the four flipped records each name the certified row they violate
and the exact excess (1/7, 1/27, 1/17, 1/12), and the other 46 verdicts keep
their original certificates unchanged.

`scripts/partial_families.py` tiers these four rows RESSAYRE at rank 11 only.
The rank-12 rows share their index sets but live in wedge^3 C^12, a different
representation, so a rank-11 certificate does not cover them and they stay
CLAIMED.

## Completeness backfill (2026-08)

The completeness audit in `scripts/audit_data_completeness.py` requires every
file under `results/data/` to carry a SHA256SUMS entry, a row here, a named
generating script, and a guard test. It found 35 orphans on first run. The rows
below close the provenance half. Each names the script that regenerates the
artifact; the script's own docstring is the detailed record, and is not
duplicated here, so there is one source of truth per artifact.

| artifact | generating script | what it is |
|---|---|---|
| `adversarial_observable_audit.json` | `independent_adversarial_audit.py` | independent adversarial audit of the observable-carrier claims |
| `census_support_geometry.json` | `census_support_geometry.py` | census-wide support geometry and RDM observability campaign |
| `compiled_sparse_benchmark.json` | `compiled_sparse_benchmark.py` | compiled sparse propagation benchmark |
| `complex_fixed_1rdm_observable_ranges.json` | `complex_fixed_1rdm_observable_ranges.py` | exact observable ranges for complex Hermitian triangles on a fixed 1-RDM fiber |
| `conditional_2rdm_body.json` | `conditional_2rdm_body.py` | the conditional 2-RDM compatibility body over the pinned Borland-Dennis fiber |
| `conditional_2rdm_program.json` | `conditional_2rdm_program.py` | steps 4 to 7 of the conditional 2-RDM program |
| `constraints/ak_thesis_rank9_10_constraints.json` | transcribed from Altunbulak's thesis; checked by `verify_constraint_source_standalone.py` | rank-9 and rank-10 constraint transcription |
| `constraints/altunbulak_appendix_a_map.json` | transcribed source map; checked by `check_manuscript_counts.py` | Appendix A source concordance for Paper 1 |
| `exact_observable_benchmark.json` | `exact_observable_benchmark.py` | exact observable propagation benchmark |
| `exact_observable_benchmark_with_control.json` | `exact_observable_benchmark.py --with-control` | the same benchmark with its dense control arm |
| `fixed_1rdm_observable_ranges.json` | `fixed_1rdm_observable_ranges.py` | exact two-body observable ranges on a pinned Borland-Dennis 1-RDM fiber |
| `generalized_observable_benchmark.json` | `generalized_observable_benchmark.py` | scaling benchmark for products of exactly observable two-level transport cells |
| `hybrid_rank_generator_prototype.json` | `hybrid_rank_generator_prototype.py` | exact lower-rank regression prototype for GPC facet generation |
| `lambda3_c7_polytopes.json` | `lambda3_c7_polytopes.py` | orbit-closure moment polytopes for wedge^3 C^d, d = 6 and 7 |
| `multichart_symplectic_propagator.json` | `multichart_symplectic_propagator.py` | multi-chart exact reduced propagator on the 29-determinant transport carrier |
| `observable_control_experiment.json` | `observable_control_experiment.py` | control experiment for the observable-manifold claims |
| `observable_manifold_geometry.json` | `observable_manifold_geometry.py` | exact combinatorics for observable-manifold tree charts |
| `rank11_open_candidate_designs.json` | `rank11_open_candidate_designs.py` | design search and exact no-design proof for the four rank-11 candidates that were OPEN |
| `rdm_determinacy_closure.json` | `fiber_dynamics_campaign.py` | four-pronged exact reduced-dynamics campaign for cyclic-window carriers |
| `rdm_method_benchmark.json` | `rdm_method_benchmark.py` | four-way propagation benchmark for the 29-state observable transport carrier |
| `schubert_bruhat_support_audit.json` | `schubert_bruhat_support_audit.py` | exact support-geometry audit for fermionic determinant carriers |
| `stage1_ressayre_3_6.json` | `stage1_generator.py` | replayable first-principles Stage 1 certificates at rank 6 |
| `stage1_ressayre_3_7.json` | `generate_fixed_n3_reference_rank.py --rank 7` | the complete fixed-N=3 rank-7 system by exact affine-flat closure |
| `transport_29_reduced_evolution.json` | `transport_29_reduced_evolution.py` | exact reduced evolution on the 29-determinant transport carrier |
| `transport_extensions.json` | `transport_extensions.py` | exact transport extensions for observable determinant carriers |
| `transport_rigidity.json` | `transport_rigidity.py` | exact carrier-preserving cross-block pair-hopping audit |

## census_master.csv (flat per-vertex census summary)

Generated by `scripts/build_census_master.py` from `states.jsonl`. It had no
generator until the completeness audit, and the shipped copy carried two
defects that a generator makes impossible to reintroduce:

- it was malformed CSV. The system field is written `(3,6)`, and the comma was
  never quoted, so every row parsed as seven fields against a six-field header;
- it was missing the entire `(3,8)` block, 761 rows against 799 vertices, and
  its DESIGN-REAL count was the pre-v0.11 value of 11.

Regenerated it is 799 rows over nine systems, 631 DESIGN-INT, 12 DESIGN-REAL,
156 INTERFERENCE, which reconciles with the README coverage table at 643 design
and 156 interference. `--check` fails if the file drifts from the ledger.

## UNREGENERABLE: cyclic_window_benchmark.json, entangling_window_hamiltonian.json

Recorded as a known gap rather than quietly carried. Both are fiber-dynamics
era artifacts with no generating script anywhere in the repository, no doc, and
no consumer: nothing reads them and nothing produces them. The completeness
audit exempts them from the generating-script requirement by name, which is why
the exemption is visible here and in `FAMILY_RULES`, and `tests/test_orphan_artifacts.py`
pins their content so they cannot change silently while the question is open.

They should be either regenerated from a recovered method or deleted. Deleting
shipped data is not something to do incidentally, so the decision is left
explicit rather than taken here.

## generator_gate1_3_7.json (the (3,7) exhaustiveness gate)

Generated by `scripts/generator_gate1_3_7.py`, scoring
`docs/prereg_generator_gate1_3_7.md` whose predictions were frozen at commit
`9c55f1b` before the run. Documented in `docs/generator_gate1_3_7.md`, guarded
by `tests/test_generator_gate1_3_7.py`. All seven predictions PASS, so `(3,7)`
is EXACT by the independent inner/outer closure: every H-row proved valid,
the H-polyhedron vertex-enumerated exactly at 10 vertices matching the
published reference, and every vertex carrying an exact attaining state.

Nothing is read off the Stage-1 manifest and believed. The four Ressayre
certificates are replayed from `(h, z)` with weights, level sets, roots and
tangent matrix rebuilt from scratch and the determinant recomputed by exact
`Fraction` elimination rather than Bareiss; all four reproduce their recorded
values (48841, 1928920, 1455387, 4817604). Each attaining spectrum is
recomputed from that state's own support and amplitudes, not from its
`integer_form`, which is the field under test.

Controls: the ordered trace-three slice alone has 13 vertices, so the four
rows cut it to 10, and dropping any single row changes the vertex set. Row 3
is the case that forced the control to be a set comparison rather than a count:
dropping it leaves 10 vertices while changing which ten they are.

This is the closure route, not the enumeration route. Candidate exhaustiveness
at `(3,7)` is bypassed rather than proved, and the manifest's
`system_completeness` field is unchanged.

## stage1_reference_series.json (blind rank-generator gates G2 and G3)

Generated by `scripts/stage1_reference_series.py`, guarded by
`tests/test_stage1_reference_series.py`, documented in
`docs/stage1_reference_series.md`. It records, per rank, the affine-flat
lattice counts, the admissible-hyperplane count, the screening partition, and
whether the generated system reproduces the published one as an oriented set.

Gate G2 is CLOSED at ranks 7 and 8: the generator produces the published
fixed-`N=3` systems (4 rows, 31 rows) BLIND, without being shown them, and the
regression compares oriented tau sets rather than counts. The rank-7 gate is
re-run live in the test suite in about eight seconds; rank 8 takes about seven
minutes and is checked from this artifact.

The enumeration is a proof, not a sample. Ranks are certified over a finite
field whose modulus is chosen to beat the Hadamard bound for the system, so a
rank computed mod `p` certifies the same rank over `Q`. The flat lattice is
therefore pinned exactly: `[1, 35, 595, 4655, 15505, 19019, 5341]` at rank 7
and `[1, 56, 1540, 21420, 147630, 467082, 565208, 166420]` at rank 8.

Gate G3 (ranks 9 and 10) is NOT closed and is recorded as operational, not as
a rejection. Rank 9 reached level 5 with 6,383,706 flats before exhausting
memory. Per-rank checkpoints are plain text and a resumed enumeration is
asserted to be identical to a fresh one, so the campaign is resumable rather
than restartable.

## census_inner_sandwich.json (P = O without candidate exhaustiveness)

Generated by `scripts/census_inner_sandwich.py`, guarded by
`tests/test_census_inner_sandwich.py`, documented in
`docs/census_inner_sandwich.md`. It closes every census system by an exact
inner/outer sandwich, the second route allowed by the claim ladder, so that
candidate exhaustiveness is never used.

`P subset O`: every stored row is re-screened live as an exact Ressayre
certificate at its own particle number, 4/4, 31/31, 52/52, 93/93, 15/15,
60/60, 125/125 and 161/161. `O subset P`: the polytope cut out by those rows
is vertex-enumerated exactly in `Fraction` arithmetic, and every vertex is
handed a certified census state whose characteristic-polynomial certificate is
re-run. Both inclusions give `P = O` at (3,6), (3,7), (3,8), (3,9), (3,10),
(4,8), (4,9), (4,10) and (5,10).

Non-circularity is the load-bearing point. The census VERTEX lists were derived
from the constraint tables, so comparing against them would prove nothing; the
attaining STATES were not, and their certificates reference no constraint
table, which is why the script re-runs them rather than trusting the vertex
list.

Two negative controls keep the outer half from being plumbing that always says
yes: perturbing one coefficient of each (4,8) row rejects 15 of 15 and
certifies 0, and screening the (4,8) rows at `particle_number=3` does not
certify them all.

The self-improving loop, in which a resisting vertex points at a missing facet,
is implemented and is recorded as BOUNDED NULL: zero vertices resisted at all
nine systems, so it has been exercised only on its trivial branch.

## polygon_diagonals.json (Theorem C, the polygon triangulation)

Generated by `scripts/polygon_diagonals.py`, guarded by
`tests/test_polygon_diagonals.py`, documented in `docs/polygon_diagonals.md`,
pre-registered in `docs/prereg_polygon_diagonals.md` and committed with its
script at `221fab8` before the run.

Reformulates Lemma 2 of `docs/holonomy_rationality.md` as a closed polygon:
a one-hop class of size `m` has `rho_AB = sum_a z_a` with `|z_a| = r_a` fixed
by the weights, so it is an `(m+1)`-gon with prescribed side lengths and the
within-class holonomies are its angles. Theorem A is the law of cosines for a
triangle; a class of size 3 is a quadrilateral and splits along a diagonal.

THEOREM C, proved and machine-checked on all 17 large classes. If a size-3
class has a rational diagonal then `z_a`, `z_c` and `D = z_a + z_b` are three
vectors in the real plane, so their Gram matrix is singular, and the vanishing
determinant is a quadratic in `t_ac` with rational coefficients and rational
discriminant. Every within-class holonomy cosine therefore lies in a
MULTIQUADRATIC field, (R1) holds, and C4 and D4 are excluded. The Gram residual
is exactly 0 on 17 of 17; 14 classes take the `gram_triangulation` route and 3
are already fully rational.

Scope 17 classes, 16 distinct states, 11 transport classes (rule R2). Scorecard
4 PASS and 2 FAIL. The failure that matters is P-B1-2: mode-coherent partial
sums were predicted rational because they are 2-RDM entries, the identification
verifies exactly 25 of 25, and 6 of them are irrational of degree 2 at (5,10)
v256, v261 and v268. So hypothesis (H2) does NOT extend from the 1-RDM to the
2-RDM, which is recorded as a standalone negative finding.

The observation that all 17 classes carry a rational diagonal is POST-HOC, not
pre-registered, and is labelled so wherever it appears. The residue, stated as
the new open problem, is to prove that every size-3 one-hop class carries a
rational diagonal or to exhibit one that does not.

Arithmetic is exact throughout: amplitudes come from the closed forms of
`states.jsonl` (never the numerical natural-orbital ledger) and rationality is
decided by `sympy.minimal_polynomial` degree, not by a tolerance. An earlier
run recorded 29 unresolved degrees AS irrational; `rational` is tri-state for
that reason and a test asserts nothing is left unresolved.

## shared_loop_rationality.json (Theorem D and Theorem C')

Generated by `scripts/shared_loop_rationality.py`, guarded by
`tests/test_shared_loop_rationality.py`, documented in
`docs/shared_loop_rationality.md`, pre-registered in
`docs/prereg_shared_loop_rationality.md` and committed with its script at
`7e112cc` before the run.

THEOREM D proves what Theorem C (`polygon_diagonals.json`) had to check. If two
pairs of a class `(A,B)` have A-side determinants one hop apart,
`u_b = u_a - C + E`, then `v_b = v_a - C + E` as well, both determinant pairs
lie in the class `(C,E)`, and the two Lemma 1 loops are the SAME integer
vector. When `(C,E)` has size 2, Theorem A applies there and the weight
monomial `M = (r_a r_b)^2` cancels identically, leaving `t_ab` rational. The
predicted value is built from the linking class alone and reproduces the `t_ab`
computed inside the original class exactly, 17 of 17, which is the cross-check
that would expose a fermionic sign error between `sigma_1 sigma_2` and
`s_a s_b`.

THEOREM C' lifts the conclusion off class size 3. The sides lie in the real
plane, so the Gram matrix has rank at most 2 and a class of size `m` needs
`m - 2` rational diagonals; a rational chain supplies them and every adjoined
radicand is rational, so the field is multiquadratic at any size. This is what
Proposition B could not give, since B's radicand carries `sin psi` from an
earlier loop and only builds a 2-tower, which may close dihedrally.

Together the two give an induction rooted at Theorem A, and 16 of the 17 large
classes are PROVED end to end with no checked arithmetic hypothesis. All 16 sit
exactly at the minimum `m - 2`, never with a spare diagonal.

Scorecard 4 PASS and 1 pre-registered FAIL. The failure is P-D-4 and it REFUTES
a conjecture rather than merely missing: "every class of size >= 3 contains a
pair linked by a class of size at most 2" is FALSE, with (3,10) v103 channel
(1,6) the counterexample, its only one-hop link sitting in the size-4 class of
the same state. That class survives only because its diagonals are rational for
another reason, which points at the honest generalisation, replacing "size 2"
with "all pairwise `t` already rational" and making the induction a mutual
recursion whose well-foundedness is the new open question.

A measurement bug was caught before scoring and is recorded: the chain search
skipped orderings with `order[0] > order[-1]` as "reverses", but reversing an
ordering changes which partial sums appear, so the reverse of a chain is
generally not a chain. That bug reported (3,10) v98 as having no rational chain
and would have manufactured a refutation of the programme's own hypothesis.
With every ordering searched, all 17 classes have one.

Arithmetic is exact throughout and reads the closed forms of `states.jsonl`,
never the numerical natural-orbital ledger.

## class_closure.json (Theorem E, and 17 of 17 closed)

Generated by `scripts/class_closure.py` from
`results/data/shared_loop_rationality.json`, guarded by
`tests/test_class_closure.py`, documented in `docs/class_closure.md`,
pre-registered in `docs/prereg_class_closure.md` and committed with its script
at `7ecec03` before the run.

THEOREM E is the statement Theorem D was a special case of. The sides of a
one-hop class lie in the real plane, so the Gram matrix has rank at most 2 and
its 3x3 minor on any triple `{a,b,c}` vanishes. With `t_ab` and `t_bc` rational
that minor is a RATIONAL quadratic in `t_ac` whose discriminant factors as
`4 (t_ab^2 - r_a^2 r_b^2)(t_bc^2 - r_b^2 r_c^2) = 4 r_a^2 r_b^4 r_c^2
sin^2(Phi_ab) sin^2(Phi_bc)`. So `t_ac` always lies in a quadratic extension by
a RATIONAL radicand, and is rational exactly when that discriminant is a
rational square, in particular whenever a linked holonomy has `cos = +/- 1`.
The size-2 hypothesis of Theorem D was only ever the base case.

THE LAST CLASS, (3,10) v103 channel (1,6), is closed. Its only one-hop link
runs to the size-4 class (3,9), so Theorem D cannot reach it. Two entries of
(3,9) are Theorem-D-proved from size-2 links at (2,6) and (1,2); Theorem E on
the triple (0,2,3) has discriminant exactly 0 because both feeding holonomies
have `cos = -1`, forcing `t_03` rational; the shared loop transports it to
(1,6) and Theorem C finishes. The triple carries
`uses_only_theorem_D_inputs: true`, so the chain consumes no measured
rationality and is grounded in Theorem A throughout.

The vanishing discriminant is not luck: `results/data/v103_fiber_count.json`
independently certifies v103's fiber as one point with holonomy
(0, pi, pi, 0, 0), so the state is real up to gauge, every holonomy sits at 0
or pi, and every sine vanishes.

Scorecard 5 PASS and 1 pre-registered FAIL. P-E-6 predicted degeneracy was a
v103 peculiarity; it is not. 17 of the 18 chained triples have discriminant 0,
spread across (3,10) v75 and both classes of v103, so degeneracy is the rule
among chained triples rather than the exception. The single non-degenerate
triple, (3,10) v75 channel (2,8) triple (0,1,2), has discriminant
`225/5903156224 = (15/76832)^2`, so both of Theorem E's modes occur in the
census: 17 close by a vanishing sine and 1 by a genuine rational square.

Closure ledger: 16 classes by Theorem D directly, 1 by Theorem E along a chain,
17 of 17 total. Every large one-hop class in the census now has multiquadratic
within-class holonomy cosines with C4 and D4 excluded, and no checked
arithmetic hypothesis anywhere in the chain.

## sine_field.json (Theorem F, the sine field)

Generated by `scripts/sine_field.py` from
`results/data/shared_loop_rationality.json` and
`results/data/holonomy_fields.json`, guarded by `tests/test_sine_field.py`,
documented in `docs/sine_field.md`, pre-registered in
`docs/prereg_sine_field.md` and committed with its script at `24f30f8` before
the run.

THEOREM F names the object that governs Theorem E. With
`A_ab = Im(z_a conj(z_b))` the signed area, the Gram entry and the area are the
real and imaginary parts of one product, `t_ab^2 + A_ab^2 = r_a^2 r_b^2`, and
Theorem E's discriminant is `Delta/4 = A_ab^2 A_bc^2`. So rationality turns on
whether `sin^2(Phi_ab)` and `sin^2(Phi_bc)` share a squarefree part, a
condition on the SINE field `Q(sin Phi)` rather than on the holonomy field
`Q(cos Phi)` the arithmetic arc studies. The sine-addition identity
`r_b^2 A_ac = A_ab t_bc + t_ab A_bc` makes that field propagate.

Scorecard 5 PASS, one of them weak. P-F-3 is the cross-check that makes the
object real rather than invented: `sin_radicands` is an independently generated
field of `holonomy_fields.json`, and every squarefree part computed here is one
of those recorded radicands, 17 of 17. P-F-4 is the substantive result and is
tested on all 54 pairs including the 28 with irrational `t`: the sine field is
a class invariant, 44 of 44 non-vacuous pairs. P-F-5 passed only weakly and the
write-up says so, because the comparison is against the union of cosine
radicands over all loops of the state rather than per loop.

Sine fields observed: 0 (degenerate) on 2 classes, then 7, 15, 23, 47, 71,
spread over 11 transport classes with 7 covering 4 and 47 covering 3. Every
nonzero area is irrational, 0 of 44, so a nondegenerate sine field is always a
genuine quadratic extension.

THE FINDING. The one class whose sine field is not contained in its recorded
cosine radicands is (3,10) v75 channel (2,8), sine field Q(sqrt 15) with NO
cosine radicands at all because both its loops have degree 1. It is also the
class that produced the single non-degenerate Theorem E triple. A programme
reading only `Q(cos Phi)` records that state as arithmetically trivial; its
arithmetic lives in the sine field.

A measurement bug was caught before scoring and is recorded: the first run
reported `in_sine_field` on 0 of 44, because `sympy.primitive_element` returns
its polynomial in a symbol of its own choosing and the degree was read against
an unrelated symbol, which returns 0 for every input. A rational-`t` pair passes
that test trivially, so 0 of 44 was impossible as mathematics. A guard test now
exercises the helper on a value genuinely outside the base field so it can
still fail.

## orbit_canonical_flats.json (Weyl-orbit canonical enumeration)

Generated by `scripts/orbit_canonical_flats.py` using
`src/gpc_census/generation/orbit_canonical.py`, guarded by
`tests/test_orbit_canonical.py`, documented in
`docs/orbit_canonical_flats.md`, pre-registered in
`docs/prereg_orbit_canonical_flats.md` and committed with its script at
`799d0c2` before the rank-8 run.

Orbit reduction is the one pruning the standing rule permits: the admissible
hyperplane set is `S_d` STABLE, so storing one representative per orbit and
expanding discards no CANDIDATE. It does NOT say two hyperplanes in one orbit
are facets together or not at all; that stronger claim is refuted in
`docs/screening_orbit_structure.md`, because screening faces the dominant
chamber and the chamber is a fundamental domain, hence exactly what the Weyl
group fails to preserve. An earlier version of this row asserted it.

Measured orbit structure of the materialised hyperplane set: 362 hyperplanes in
8 orbits at rank 6, 5,341 in 19 at rank 7, 166,420 in 56 at rank 8, reductions
45x, 281x and 2,972x, with `images_outside_set` zero at all three so the action
is closed.

The augmentation enumerator holds only orbit representatives and never
materialises the lattice. Expanding each orbit by `d! / |Aut|` rebuilds the
materialised flat counts EXACTLY at every level for ranks 6, 7 and 8. At rank 9
the materialising enumerator died at level 6 having checkpointed levels 1 to 5;
the orbit expansion reproduces those five stored counts (84, 3,486, 78,274,
968,121, 6,383,706) and continues past the point of death, giving the full
rank-9 lattice [1, 84, 3486, 78274, 968121, 6383706, 20605599, 27392341,
10004154]. So **(3,9) has 10,004,154 admissible hyperplanes**, computed here for
the first time, through 231 orbits with a peak of 494 at any level, in 407
seconds. Rank 8 takes 2.9 seconds against 125 seconds materialised.

**(3,10) is now recorded too**, which it previously was not: the write-up quoted
889,205,792 while this artifact stopped at rank 9. The cause was a bug in the
measuring script, which computed augmentation and then wrote the payload only
inside the `--ranks` loop, so requesting augmentation alone produced no file at
all. The script now writes unconditionally. The record is orbits
[1, 1, 3, 12, 49, 189, 691, 1840, 2705, 1337] expanding to
[1, 120, 7140, 241150, 4823910, 56904687, 379791510, 1315196575, 1994358765,
889205792], so **(3,10) has 889,205,792 admissible hyperplanes through 1,337
orbits**, with the peak of 2,705 at codimension two rather than at the top.

Each augmentation record now carries `levels_restored_from_checkpoint` and
`timing_is_a_full_run`. A resumed run reports a wall time of nearly nothing,
which times nothing, and shipping that as a measurement would be worse than
shipping no number: the (3,10) figure of 520 seconds covers only its last two
levels, and the record says so. The file also carries machine metadata and a
`augmentation_sha256` over the augmentation block.

Costs quoted above are for the canonicalizer as it stood then. Codimension-one
flats are now keyed by their exact primitive normal, a sort rather than a
search, which took (3,8) from 14.7 to 4.8 seconds and (3,9) from 228.6 to 51.2,
with the ladders and every orbit size checked against the hypergraph route.

Scorecard 5 of 5 PASS, including the narrow externally proposed band for the
rank-8 orbit count.

The rank-8 level vector [1, 56, 1540, 21420, 147630, 467082, 565208, 166420]
now has three independent derivations: the repository's own
`stage1_reference_series.json`, an external numpy reimplementation over
`GF(2^31 - 1)`, and this run.

A convention trap is recorded and pinned by a test because the wrong answer is
plausible. Hyperplanes are stored with first nonzero entry positive, so a
permuted image must be re-normalized; without that the action sends 201 of 362
images outside the set at rank 6 and the orbit count reads 15 instead of 8, and
35 instead of 19 at rank 7. Those are valid orbit counts of a valid action on a
set that is not the hyperplane set.

This enumerates hyperplanes, not facets. Each representative still needs
Ressayre screening and exact reduction, and no (3,9) GPC system is produced
here.

## screening_orbit_structure.json (screening is NOT a Weyl-orbit invariant)

Generated by `scripts/screening_orbit_structure.py`, guarded by
`tests/test_screening_orbit_structure.py`, documented in
`docs/screening_orbit_structure.md`.

REFUTES the natural next step after `orbit_canonical_flats.json`. Orbit
reduction is lossless for ENUMERATING candidates, and the hypothesis was that
one representative per orbit could be screened and the verdict transported.
That is false: at rank 7, 25 of the 35 oriented-tau orbits carry mixed
verdicts, several containing both `certified` and `trace_rejected`. The reason
is structural, not an implementation defect. The dominant chamber is a
fundamental domain and is therefore precisely what the Weyl group does not
preserve, so a chamber-facing condition cannot be an orbit invariant; the Weyl
invariance of the moment cone is a statement about the ambient cone.

WHAT SURVIVES. The Ressayre trace test compares negative roots with
`h_j - h_i < 0` against exterior weights strictly below the hyperplane. The
second count is `S_d` INVARIANT, verified on every row at ranks 6, 7 and 8 with
zero failures. The first is exactly the INVERSION NUMBER of `h`, verified
directly against `_negative_root_set`, and inversions spread over a whole
Mahonian range within one orbit. So the test reads `inv(sigma h) == B` with `B`
an orbit invariant, which means `B` costs one evaluation per ORBIT instead of
one per row and each row then costs only an inversion count.

Measured: 724 rows in 15 orbits with 64 trace survivors (8.84%) at rank 6,
10,682 in 35 with 549 (5.14%) at rank 7, and 332,840 in 109 with 6,607 (1.98%)
at rank 8. The 549 is an exact cross-check against
`docs/fixed_n3_generator_methodology.md`, which records 10,133 trace failures
out of 10,682. The survivor fraction is falling, which is what makes the
expensive determinant stage shrink as a share of the work.

`docs/orbit_canonical_flats.md` has been corrected: it previously said orbit
invariance of screening "is true for validity", and it also justified orbit
reduction by claiming hyperplanes in one orbit are facets together or not at
all. Both were wrong. The enumeration reduction stands on the `S_d` STABILITY
of the candidate set, which is verified separately.
## symplectic_invariants.json (lattice cones and exact stabilizers, all 799 rows)

EXACT. Generated by `scripts/symplectic_invariant_census.py`, pre-registered in
`docs/prereg_symplectic_invariants.md`, documented in
`docs/symplectic_invariants.md`, guarded by
`tests/test_symplectic_invariants.py`, and replayed with no project import by
`scripts/verify_symplectic_invariants_standalone.py`.

Two presentations of each census object, computed independently of the
combinatorial one already shipped.

LATTICE. The tangent cone at each vertex, built from the edge directions of the
polytope at that vertex. Edges are identified combinatorially from the V- and
H-representations: `[v, w]` is an edge exactly when the vertices saturating
every facet through both are exactly `v` and `w`. Each ray is reduced to its
primitive generator in the saturated lattice `Z^d` intersected with the
direction space of the affine hull, and the ray matrix against a basis of that
lattice gives the Smith invariant factors, the lattice index, and
unimodularity. The lattice basis is obtained by column-style Hermite reduction,
so it is saturated by construction rather than by a denominator-clearing
heuristic.

SYMPLECTIC. The exact projective stabilizer `Stab_{U(d)}([psi])` of the
certified state, its orbit dimension, the commutant of the state's own 1-RDM,
and the dimension of the commutant orbit through the state. Ranks are exact
over the algebraic number field the amplitudes generate, never numerical.

FIBER QUALIFICATION. Every stabilizer number is UNREDUCED and FIXED-RHO. No
first-order fiber tangent dimension is reported: `ker d(mu)` has dimension at
least `2*binom(d,n) - d^2` for trivial reasons at these ranks, so such a number
is a vacuous upper bound, and the prereg says so before the run rather than
after.

CONVENTION TRAP, found by the artifact's own internal check and pinned by a
test. Differentiating `rho_pq = <psi|a_p^* a_q|psi>` along the flow of
`X in u(d)` gives `d(rho) = [rho, X^T]`, so the subalgebra preserving `rho`,
which must contain the whole projective stabilizer, is the commutant of `rho^T`
and not of `rho`. The two agree identically for the 645 states whose 1-RDM is
real diagonal, and differ as subspaces for the other 154. Using `rho` produced
a commutant that failed to contain the stabilizer at `(3,10)#73`
(`stab_dim = 6` against `commutant_stab_dim = 4`), which is impossible, and
that single row is what exposed the error. The forced identity
`stab_dim == commutant_stab_dim` is now checked on every row and asserted by
the guard test, and `commutant_dim` is separately checked against the sum of
squared eigenvalue multiplicities of the vertex spectrum, which is an
independent confirmation that each certified state's 1-RDM really carries the
census spectrum.

## quantization_multiplicities.json (the representation-theoretic presentation)

EXACT. Generated by `scripts/quantization_multiplicity.py`, same
pre-registration, same guard test, same standalone replay.

For a vertex `lambda` with denominator `q` and `k` a multiple of `q`, the
multiplicity of the irreducible `U(d)` representation of highest weight
`k*lambda` in `Sym^k(wedge^n C^d)` is computed by the alternating Weyl formula
over exact integer weight counts, where the weight multiplicity is the number
of multisets of `k` occupation vectors with a given sum. Both halves are
integer counts, so the multiplicity is exact.

The method is validated on decompositions that can be checked by hand:
`Sym^1(wedge^n C^d)` returns multiplicity one at the fundamental weight and
zero elsewhere, and `Sym^2(wedge^2 C^4)` returns exactly
`V_(2,2,0,0) + V_(1,1,1,1)`. The standalone verifier additionally re-derives
every multiplicity with a dictionary generating-function DP instead of the
memoized suffix search, and checks the Weyl dimension identity
`sum_mu m(mu) dim V_mu = dim Sym^k(wedge^n C^d)` at several `(n, d, k)`.

Coverage is `(3,6)` and `(3,7)` under a wall-clock budget, not the whole
census: the weight count grows quickly in `k` and `d`. The `k` values actually
reached are recorded per vertex in `k_tested`, and vertices truncated by the
budget are named in the scorecard rather than silently dropped.

## quantization_characters.json (the period is a finite isotropy character)

EXACT. Generated by `scripts/quantization_character.py`, pre-registered in
`docs/prereg_quantization_character.md`, documented in
`docs/quantization_character.md`, guarded by
`tests/test_quantization_character.py`, and replayed with no project import by
`scripts/verify_quantization_character_standalone.py`.

Explains `results/data/quantization_multiplicities.json`. For each of the 14
`(3,6)` and `(3,7)` vertices, enumerates the `lambda`-preserving permutations
fixing the certified attainer's line, `wedge^n(P_sigma) psi = c psi`, and
computes the least `k` divisible by the spectrum denominator `q` with

    c^k * prod_i det(sigma|block_i)^(-k * lambda_i) = 1.

The lcm over those elements is the predicted period. Result: **14 of 14 exact
matches** against the measured periods, including all four period-doubling
vertices, each explained by a single explicit permutation.

The searched group is a SUBGROUP of the full stabilizer (permutations only, no
phased or non-monomial elements), so the predicted period can only ever divide
the measured one. Equality is therefore the falsifiable content and
divisibility is a correctness gate, which is how the pre-registration labels
them.

CONVENTION TRAP, found by that gate and pinned by a regression test. The
exponent on Levi block `i` is `k*lambda_i`, and the first version read it from
`integer_form[i]`, indexing an orbital-indexed array by a block counter. The
two agree only when every block is a singleton. The bug manufactured
obstructions at `(3,6)#0`, `(3,7)#0` and `(3,7)#1`, where the predicted period
came out strictly larger than the measured one, which the derivation forbids.
It left the four genuinely doubled vertices correct, so the equality prediction
alone would not have caught it.

The standalone replay recomputes the wedge action through `n x n` MINORS of the
permutation matrix, `(wedge^n h)_{T,S} = det(h[T,S])`, rather than through the
generator's combinatorial sorting sign, and reproduces every predicted period
and every element count with zero failures.

Not proved here: that a degree-`k` semi-invariant of weight `k*lambda` fails to
vanish at the certified attainer whenever the multiplicity is nonzero. That
hypothesis is stated in the pre-registration and its consequence is what the
artifact tests.

### census_predictions block (added in the same run)

`--census` additionally predicts the period for all 799 census vertices, none
unresolved, in 100 seconds. The character side needs only the attainer, so it
is not capped at rank 7 the way the multiplicity side is.

The ratio `predicted_period / q` is **1 at 745 vertices and 2 at 54, and never
anything else**, so the isotropy obstruction is exactly a `Z/2` across all nine
systems.

These rows are PREDICTED, not measured, and each carries a `verified` flag that
is true only for the fourteen vertices with a measured multiplicity sequence.
Divisibility (`predicted` divides the true period) is derived and holds at all
799 under the stated semi-invariant hypothesis; equality is verified at 14 and
assumed at the other 785. Do not quote the 785 as measured periods.

A support prefilter makes this tractable: `P_sigma` is invertible, so
`wedge^n(P_sigma) psi` is nonzero and can be a scalar multiple of `psi` only if
`sigma` permutes the support. Testing that as a set operation before any exact
amplitude work is what brings the worst case (a vertex whose Levi is all of
`S_10`, so 3,628,800 candidates) down to about 20 seconds.

## symplectic_slices.json (Marle-Guillemin-Sternberg local model, PARTIAL)

EXACT, and PARTIAL by design. Generated by
`scripts/symplectic_slice_atlas.py`, pre-registered in
`docs/prereg_symplectic_slice.md`, documented in `docs/symplectic_slice.md`,
guarded by `tests/test_symplectic_slice.py`.

The symplectic slice `V_x = ker d(mu)_x / T_x(K_mu . x)` at a certified state.
This is the object `docs/symplectic_invariants.md` lacked: the raw
`ker d(mu)_x` is dominated by trivial ambient directions and is a vacuous upper
bound, while `dim V_x = 0` certifies that the reduced germ is a point.

Completed: `(3,6)#0` (Slater), `ker d(mu) = 20`, `dim V_x = 20`; and `(3,6)#3`
(uniform Borland-Dennis), `ker d(mu) = 19`, `dim V_x = 0`, so that vertex is
CERTIFIED RIGID. Together they show that a nonzero slice does NOT mean a
positive-dimensional reduced space: the Slater's reduced space is a point
despite a 20-dimensional slice.

Unattempted within the compute budget and listed by name in the artifact's
`unfinished` field, never dropped: v_B `(4,9)#65`, v89 `(3,10)#89`, v103
`(3,10)#103`. Exact symbolic linear algebra costs `H = 126` and `H = 120` there
against `H = 20` at rank 6, and the bottleneck is an `rref` on a `2H x 2H`
symbolic matrix. Reproduce the shipped artifact with
`--skip "(4,9)#65" "(3,10)#89" "(3,10)#103"`.

Three forced identities are checked on every row and all hold:
`rank d(mu)_x = dim(K.x)`, `dim ker d(mu)_x = 2(H-1) - dim(K.x)`, and
`dim T_x(K_mu.x) = dim K_mu - dim K_x`. The first two failed during
construction and caught a real bug: pivot COLUMNS of an `rref` were used to
select independent ROWS, which dropped `rank d(mu)` from 18 to 17 at the Slater
vertex and inflated the kernel to 21, which would have shifted every slice
dimension by one.

NOT reported: any reduced-germ dimension where the slice is nonzero. The
shortcut `dim V_x - 2 dim(K_x . v)` at a generic `v` in `V_x` is REFUTED, not
merely unproven: it needs a generic point of `mu_V^{-1}(0)`, and at the Slater
vertex it returns -10.

## ordered_representation_stability.json (ordered representation stability)

EXACT. Generated by `scripts/ordered_representation_stability.py` from the
published constraint tables in `gpc_census.constraints` and the census vertex
files, guarded by `tests/test_ordered_representation_stability.py`,
pre-registered in `docs/prereg_ordered_representation_stability.md` and written
up in `docs/ordered_representation_stability.md`. All rational arithmetic is
exact; no solver and no floating point enter.

Scope is the nine published systems (542 rows) plus their six exact
particle-hole duals (365 rows), 907 rows in all. Duals are marked `primary:
false` and are never counted as independent evidence.

It records, per row: the primitive homogeneous tau, its canonical affine form,
width, spread, height, a descent label (STRUCTURAL, PADDING, FROZEN-CORE,
PRIMITIVE) and, for every inherited label, the donor row and the round-trip
witness that certifies it. No label is assigned from coefficient resemblance.

Load-bearing results. Terminal restriction is total (724 valid images).
The tight padding extension and the tight frozen-core extension are exact
sections (620 of 620 round trips) and carry published facets to published
facets in 612 of 612 nonstructural instances, once the structural chamber rows
and the non-full-dimensional Borland-Dennis system (3,6) are excluded by name.
All 514 inherited rows replay exactly from their primitive cores. Every one of
the 907 published rows is certified a genuine facet of its ordered slice, by an
exact affine-rank computation on the tight vertices, so the facet counts the
disproof relies on are not inflated by redundant rows. Theorem 1's
non-degeneracy hypothesis is certified at all 15 systems by exhibiting the
barycentre of the vertex list as an interior point with every coordinate
strictly positive; the Weyl-averaging shortcut is unavailable here and the
`nondegeneracy` block is what replaces it. No nonstructural row is
permutation stable: 0 of 901, the only six survivors being the structural Pauli
row. The Weyl saturation of the moment polytope is not
convex, with the certificate (1^(N-1), 1/2, 1/2, 0^(d-N-1)) recorded at all
nine primary systems.

The scorecard is part of the artifact so that verdicts are read from data
rather than retyped. Three of the eleven pre-registered predictions FAILED (P5
monotonicity of primitive counts, P7 height growth, P8c holdout window) and the
failures are findings, kept unedited per standing rule R5.

Conditionality inherited from the tables: rank-10 rows are conjecturally
complete per AK2008, so any rank-10 count here carries that condition. The
disproof of finite generation at generation degree 8 or below does not, since
it needs only |F(3,9)| = 52 > 31 = |F(3,8)|.

## bdr_constructor_comparison.json (what an external candidate generator can remove)

Generated by `scripts/bdr_constructor_comparison.py`, scoring
`docs/prereg_bdr_constructor_comparison.md` whose predictions were frozen at
commit `2e0216e` before the run. Documented in
`docs/bdr_constructor_comparison.md`, guarded by
`tests/test_bdr_constructor_comparison.py`.

The Bulois-Denis-Ressayre backend (`ea-icj/moment_cone`, pinned revision
`7ab347d9a7837d68d92bde9fac74606913f395ca`, arXiv:2505.08812) was NOT run and
no external figure appears in the artifact. Its code host, its landing page,
arXiv and HAL are all refused by this session's egress policy, it is not on
PyPI, and it needs SageMath. The benchmark is labelled INCOMPLETE and the
artifact carries `external_backend_executed: false`.

Measured instead, at `(3,6)` screening only and at `(3,7)` and `(3,8)` in full:
three paths through the local pipeline, stage attributed into enumeration,
screening and composition. `full` is the orbit-native construction. `oracle`
supplies the published minimal rows directly. `birationality_prefilter`
(`added_after_scoring: true`) keeps local enumeration but removes every
determinant-zero row before screening, modelling the one BDR step with no local
counterpart.

At `(3,8)`, on `Linux-6.18.5-fc-v20-x86_64` with CPython 3.14.0rc2 and a single
worker: full 16.00 s (enumeration 6.25, screening 7.64, composition 2.11),
prefilter 8.80 s for 1.82x, prefilter with free external enumeration 2.55 s for
6.27x, oracle 0.98 s for 16.38x. The 6.27x figure is the non-circular bound,
because a generator cannot know which certified rows survive reduction without
running the reduction. Wall clock on one machine, never averaged with another.

Recertification, the part that is exact rather than timed: all 36 published
nonstructural rows of `(3,6)`, `(3,7)` and `(3,8)` convert to primitive `tau`,
screen as `certified` by deterministic evaluation, replay and bind their
certificates, and match a local orbit. Per-row certificate SHA256 values are
stored. No external verdict is accepted by citation.

Pre-registered prediction P5 FAILED: enumeration is 39.1 percent of the `(3,8)`
wall clock here, not the majority, so screening is the dominant stage on this
machine. `docs/orbit_native_constructor.md` records the opposite on its own
machine, where the same construction cost 66.0 s. That claim is now OPEN
pending a re-measurement there.

Also recorded: the oracle ceiling FALLS from 21.41x at rank 7 to 16.38x at
rank 8 while the row-reduction ratio rises from 137 to 213, because the oracle
path is itself dominated by composition, which scales with the retained facet
count. Two points are a trend, not a theorem, and no claim uniform in `d` is
made.

## facet_index_stability.json (what is stable, and the coefficient-one refutation)

Generated by `scripts/facet_index_stability.py`, scoring
`docs/prereg_facet_index_stability.md` whose predictions were frozen at commit
`ad55d4c` before the run. Documented in `docs/facet_index_stability.md`, guarded
by `tests/test_facet_index_stability.py`.

Motivated by the measured result in `docs/bdr_constructor_comparison.md` that
the stage with the worst long-run scaling is redundancy elimination, because
composition is the floor no external tool can remove and it grows with the
retained facet count.

Part B, PREDICTIVE and the load-bearing half. The production pipeline defers
exact cyclic-Schubert evaluation until after redundancy elimination, so the
coefficients of the rows reduction REMOVES had never been computed at any rank.
Computing them for every certified row at `(3,7)` and `(3,8)`, 49 and 137 rows,
REFUTES the AK2008 coefficient-one criterion as a facetness test: every facet
has coefficient one (4 of 4 and 31 of 31), and so do 23 of 45 and 36 of 106
removed rows. Coefficient one is necessary and not sufficient, with 59 exact
counterexamples. No coefficient zero, no ill-formed cyclic problem, no row
unresolved by its 30 s budget. Distinct coefficients on removed rows are 1, 2 at
rank 7 and 1, 2, 3, 4, 6, 8, 9, 12, 18 at rank 8.

The forward direction surviving is what makes coefficient one a sound PREFILTER
rather than a criterion. Recorded under `coefficient_one_prefilter` with
`added_after_scoring: true`: it cuts the reduction input from 49 to 27 and from
137 to 67, gives 1.66x and 1.79x on the reduction stage, and returns an
identical retained set. NOT gate-eligible: soundness rests on an observation at
two ranks, not on the theorem that every facet has coefficient one.

Part A, LANDED and not scored under rule R4, because the tables were computed in
scratch before the pre-registration existed. The artifact records scratch and
landed values side by side and they agree exactly. Trailing-zero padding is not
validity preserving on facets: padded lower-rank rows are violated at the next
rank in 7 of the 8 published maps where validity is decidable, and a padded
facet is never merely redundant, the strictly-interior count being zero in every
map. `(5,8)` to `(5,9)` is reported validity UNAVAILABLE because `(5,9)` has a
constraint list but no precomputed vertex file. Across all twelve published
systems the facet count runs 1 to 161 while distinct sorted-`tau` shapes run 1
to 21, a ratio at or below 0.29 everywhere and at or below 0.22 wherever there
are more than four facets. The `N=3` entry bound of 7 at ranks 8 to 10 does NOT
transfer across particle number, reaching 15 at `(4,10)` and 34 at `(5,10)`, as
`tau_i = N a_i - b` implies, so no entry bound uniform in `N` is claimed.

Conditionality: the rank-9 and rank-10 constraint lists are conjecturally
complete per AK2008, so every statement resting on them is conditional on that
completeness.

Merge-time reconciliation with `ordered_representation_stability.json`. That
campaign reports a padding extension that is total and exact, 612 of 612, which
contradicts the padding failure above only until one notices the maps differ:
its `pi_pad` appends the largest last entry keeping the row valid, this one
appends zero in affine coordinates. `pi_pad` is IMPORTED by
`scripts/facet_index_stability.py` rather than reimplemented, so the two
campaigns cannot drift. Measured in all 8 decidable maps across three particle
numbers: a trailing-zero padded facet lands in the higher facet list EXACTLY
when the trailing-zero extension coincides with the tight extension, counts
0, 4, 10, 37, 0, 8, 19, 0 on both sides. The trivial extension succeeds
precisely where it was already tight.
